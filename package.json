{
  "name": "fleet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Run coding-agent jobs in containers on your own AWS account.",
  "engines": {
    "node": ">=23.6"
  },
  "files": [
    "src",
    "schemas",
    "presets",
    "examples",
    "integrations",
    "docs"
  ],
  "bin": {
    "fleet": "src/cli/main.ts"
  },
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
