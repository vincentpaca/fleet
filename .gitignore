node_modules/
.terraform/
*.tfstate*
.terraform.lock.hcl
.fleet/out/
coverage.lcov
# Agent worktrees the coding harness checks out next to the repo. Operator-
# local scratch, and each one is its own git repo.
.claude/worktrees/
.DS_Store
